<Window x:Class="PhotoLibrary.View.PhotoGalleryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource mainViewModelLocator}, Path=PhotoGalleryViewModel}"

        Title="Photo Library" Height="600" Width="600">

    <Window.Resources>
        <DataTemplate x:Key="PhotoGalleryDataTemplate">
            <Grid>
                <Image Width="160" Height="160" HorizontalAlignment="Stretch">
                    <Image.Source>
                        <BitmapImage DecodePixelWidth="150" DecodePixelHeight="150" UriSource="{Binding ImageUri}" />
                    </Image.Source>
                </Image>
            </Grid>
        </DataTemplate>

        <ItemsPanelTemplate x:Key="PhotoGalleryItemsPanelTemplate">
            <UniformGrid Columns="3" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        </ItemsPanelTemplate>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <ListBox x:Name="PhotoGallery" ItemsSource="{Binding AllPhotos}" ItemTemplate="{DynamicResource PhotoGalleryDataTemplate}" ItemsPanel="{DynamicResource PhotoGalleryItemsPanelTemplate}"/>
        </Grid>
    </ScrollViewer>
</Window>
